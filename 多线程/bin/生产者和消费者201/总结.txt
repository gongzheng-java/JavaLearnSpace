  
  
  
   总结：
            该示例很好的解决了“线程之间的通讯”示例中的问题，但是任然存在缺陷，即使用notifyAll()方法唤醒线程的的时候将唤醒所有线程（会出现抢夺资源的现象），
       而我们的需求是只需要唤醒对方线程即可。但是该程序中使用synchronized锁，以及object对象的wait()和notifyAll()方法来控制资源，这样的统一操作，
       无法做到针对性控制不同的线程资源。JDK1.5后解决了这一问题，参考示例“生产者和消费者2”.